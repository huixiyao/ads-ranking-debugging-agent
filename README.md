# ads-ranking-debugging-agent
A tool to analyze and optimize Ads Ranking metrics based on simulation data


## Quickstart

1. Put Avazu `train.csv` under `data/` (not tracked by git).
2. Run Day2 metrics pipeline:

```bash
cd src
python3 run_metrics.py

```